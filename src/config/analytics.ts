// No tracking scripts or cookies are loaded. Future providers must be mounted
// only after explicit consent; keep IDs and provider configuration here.
export const analytics = {
  enabled: false,
  googleAnalyticsId: "",
  metaPixelId: "",
  linkedInPartnerId: "",
};
